define 

cve sub entity,
    owns name,
    owns status,
    owns description,
    owns vote,
    owns comments;
    
name sub attribute, value string;
status sub attribute, value string;
description sub attribute, value string;
content sub attribute, value string;
phase sub attribute, value string;
vote sub attribute, value string;
comments sub attribute, value string;

